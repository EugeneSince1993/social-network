В курсе (2018 г) React.js ver 16.
У меня (2021 г) React.js ver 17.

JSX - отдельный синтаксис ("язык"). Он позволяет писать HTML внутри JavaScript. По факту это JS.

Стрелочная функция App возвращает разметку.
В JSX-разметке теги должны быть обернуты в 1 корневой тег (div).